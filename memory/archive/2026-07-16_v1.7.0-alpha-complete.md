# Project State

> Last updated: 2026-07-16

## Version

- **Base**: `1.7.0-alpha`
- **Latest tag**: `v1.6.3`

## Build Status

| Platform | Status | Notes |
|----------|--------|-------|
| Linux (desktop) | ✅ Builds + runs | GCC/C++17, raylib 5.0, 0 warnings |
| Android (APK) | ✅ Builds + verified | VSYNC working, lifecycle survives surface loss |
| Windows (cross) | ✅ Builds | `dist/theword-1.6.3-alpha-windows-x86_64.zip` |
| WASM | ⏳ Not verified | Previously built |

## Tests

- **Total**: 76 test cases, 267 assertions
- **Passing**: 74/76 (263/267 assertions)
- **Failing**: 2 (both locale-related — system runs pt_BR, tests expect English)
- **Run**: `./build/theword_test`

## Current Focus

Implementing v1.7.0-alpha "Reading Experience" features.

### What's Next

| Workstream | Status |
|------------|--------|
| Input System Refactor + Bug Fixes | ✅ Complete (v1.6.1-alpha) |
| Radial Menu + Android Lifecycle | ✅ Complete (v1.6.2-alpha) |
| Android VSYNC + Idle Drain | ✅ Complete (v1.6.3-alpha) |
| App.cpp Refactor — Run() extraction | ✅ Complete |
| Copy Verse Polish | ✅ Complete |
| Immersive / Clean Mode | ✅ Complete |
| Verse Flow Fix (no forced line breaks between verses) | ✅ Complete |
| Bible ID Fix (129→3034) | ✅ Complete |
| Open Where You Left Off | ✅ Complete |
| Footnote Display | ✅ Complete |

### What Changed (v1.7.0-alpha)

| File | Change |
|------|--------|
| `App.h` | Added `HitInfo` forward decl, `longPressHandled_` member, 10 private method decls, `CopySelection()` decl, `ChapterData` forward decl |
| `App.cpp` | Refactored `Run()` into `WireInputCallbacks()`, `HandleShortcuts()`, `MainLoop()`, 7 `On*` methods; citation prefix + verse range in `AssembleSelectedText()`; `CopySelection()` helper; inline copy → helper calls; Ctrl+C shortcut; Toast draw loop wiring; footnote hit-test lambda + onFootnoteTap + tap-away/Escape dismiss + DrawFootnotePopup |
| `UIManager.h` | Toast API + FootnotePopup API (Show/Draw/Hide/IsActive) |
| `UIManager.cpp` | Toast implementation + FootnotePopup rendering with word-wrap and screen-edge clamping |
| `LayoutEngine.cpp` | Group consecutive VerseText segments; insert `[n]` footnote marker spans after verse number |
| `App.cpp` | Fixed Bible ID from 129 (access denied) to 3034 (BSB) — online API now works on mobile |
| `AGENTS.md`, `YouVersion API.md` | Updated Bible ID references |
| `memory/State.md` | Updated for v1.7.0-alpha development start |
| `memory/Active.md` | v1.7.0-alpha checklist (all features done); Footnote Display checklist updated |
| `the-word-docs/04-planning/Release Plan.md` | Added v1.7.0-alpha feature sections |
| `the-word-docs/xx-user-notes/License Agreement BIBLICA.md` | **New** — complete Biblica license analysis |
| `App.h` | Added `float pendingScrollY_` |
| `CMakeLists.txt` | Bumped version 1.6.3 → 1.7.0 |
| `ChapterProvider.h` | +Footnote struct, +SegmentType::FootnoteMarker, +footnotes vec, +Span::footnoteIndex |
| `Theme.h` | +3 footnote colors (DOC_FOOTNOTE_CALLER, _POPUP_BG, _POPUP_TEXT) |
| `USFMParser.h` | StripFootnotes → ExtractFootnotes decl |
| `USFMParser.cpp` | ExtractFootnotes impl (tracks \c/\v, extracts \fr/\ft, distributes post-parse); ParseBook updated |
| `BibleClient.cpp` | StripFootnotes → ExtractFootnotes (self-contained, tracks yv-v); ParseParagraphContent/ParsePoetryLine updated |
| `Renderer.cpp` | +FootnoteMarker case in DrawSpan, superscript Y offset |
| `InputHandler.h` | +onFootnoteTap + hitTestFootnoteFn (ctor param + member) |
| `InputHandler.cpp` | Footnote hit test in RunUnifiedFSM before word checks |
| `tests/test_main.cpp` | BibleClient footnote test updated (extract + verify); USFM footnote test added (MAT 1) |

### What Changed (v1.6.2-alpha)

| File | Change |
|------|--------|
| `RadialMenu.h/cpp` | Sector-based hit detection (eliminates dead zones), debug tap overlay |
| `App.cpp` | Highlight recolor fix (`HighlightOverlapping`), debug tap recording, radial sector click handling |
| `Highlighter.h/cpp` | `HighlightOverlapping()` — overlap-based lookup for recolor |
| `UIManager.h/cpp` | `RecordDebugTap()` / `DrawDebugTap()` |
| `InputHandler.h/cpp` | `ResetState()` for lifecycle resume, `SetTouchDown()` guard |
| `Platform.h/cpp` | `Platform::DestroyRequested()` — surface loss detection |
| `BibleBooks.h` | Added `EXO` → `Êxodo` / `Exodus` (locale-aware mapping) |
| `CurlHttpClient.cpp` | User-agent header for API requests |
| `scripts/build-android.sh` | Multi-ABI APK packaging |
| `memory/Active.md` | Radial menu + lifecycle checklist |
| `the-word-docs/04-planning/archive/InputRefactor.md` | **New** — archived design doc |

### What Changed (v1.6.3-alpha)

| File | Change |
|------|--------|
| `Platform.cpp` | EGL VSYNC logging + `eglSwapInterval()` with redundant display handle |
| `cmake/patches/raylib-5.0-android-char-input.patch` | Uncommented `eglSwapInterval` in `InitGraphicsDevice()` and added after `eglMakeCurrent` in rebind path |
| `App.cpp` | Time-based idle drain — `DrawGrid()` throttle at ~5fps when nothing changes |
| `Config.h` | `IDLE_DRAIN_MIN_FRAME_TIME` constant |
| `memory/Active.md` | VSYNC investigation checklist (now resolved) |

### Problems Solved

| Problem | Status |
|---------|--------|
| P1–P12 (v1.6.1 input refactor) | ✅ — see archived design doc |
| P13 (radial dead zones) | ✅ — sector-based hit detection in v1.6.2-alpha |
| P14 (highlight recolor duplicates) | ✅ — `HighlightOverlapping` in v1.6.2-alpha |
| P15 (Android surface loss crash) | ✅ — `DestroyRequested()` check in v1.6.2-alpha |
| P16 (Android VSYNC not working) | ✅ — raylib patch + Platform.cpp logging in v1.6.3-alpha |
| P17 (idle power drain) | ✅ — time-based idle drain cap in v1.6.3-alpha |

## Known Issues

- **Android bottom bar input corruption** — can get stuck after resume (low-severity, deferred)
- Locale-dependent test failures (pt_BR locale) — low priority
- Complex IME composition (CJK, emoji) — deferred to backlog

## Active Decisions

| Decision | Rationale |
|----------|-----------|
| Input refactor before v1.6.0 tag | Click system accumulated too many patches |
| Unified FSM replaces dual FSMs | Eliminates duplication and P2-divergence |
| Semantic event callbacks replace lifecycle callbacks | Removes touch-down vs touch-up race (P1) |
| App.cpp becomes single decision-maker | Consolidates accumulator + radial lifecycle (P4) |
| Callbacks over EventBus for gesture transport | Simple, sufficient; EventBus can wrap later |
| Dual roadmap system: `docs/` (high-level) + `memory/` (active) | Separates stable reference from sprint ephemera |
| Sector-based radial hit detection | Eliminates dead zones between buttons |
| raylib patch for Android VSYNC | Upstream `eglSwapInterval` commented out in 5.0 |
